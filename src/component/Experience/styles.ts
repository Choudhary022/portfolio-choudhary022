import {
    Box,
    styled
} from "@mui/material";

export const WorkExperienceContainer = styled(Box)(() => ({
    display: "flex",
    flexDirection: "column",
}))

export const DetailContainer = styled(Box)(() => ({
    padding: "10px",
    boxShadow: "0 4px 6px rgba(0, 0, 0, 0.3)"

}))




